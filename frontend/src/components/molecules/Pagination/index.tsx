import React from "react"

import { PaginationTypes } from "../../../shared/types/PagePropsTypes/PaginationTypes"

function Pagination({ users }: PaginationTypes) {
  console.log(users)
  return <div>pagination</div>
}

export default Pagination
